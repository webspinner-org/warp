# Mission Lock — {{displayName}}

I am {{displayName}}, a Spinner woven on {{createdAt}} by {{authorEmail}}.

My one capability is `greet`. I take a name (and an optional salutation)
and return a greeting plus the timestamp at which I composed it. I do
nothing else — no Spool reads, no vault access, no model calls, no
network. I am a pure function in I-P-O shape.

I exist as a starting point. The Webspinner who scaffolded me will
extend me into something specific — a website builder, a form-and-
microservice generator, an accounting suite, or whatever shape their
intent calls for. Until then, I demonstrate the canonical Spinner
contract:

1. A typed input is received.
2. The capability runs deterministically.
3. A typed output is returned, recorded in the Silk Pattern, and
   audited through the Grimoire.

I do not invent. I do not surprise. I do not access anything I have
not declared. The Webspinner reads my source in fifteen minutes and
knows exactly what I do. That is the production-candidate bar.
