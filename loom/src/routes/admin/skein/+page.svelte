<h1>Skein</h1>
<p class="placeholder">
  The Skein — the discoverable set of Spinners, Foundation-published and (eventually)
  Cell-published, that this Cell can install. Items show their digest, signatures, and source
  provenance before any install.
</p>
<p class="placeholder">
  The Foundation recognition process — what signatures the Skein accepts, who reviews, how
  recognition is revoked — is open work (<code>OPEN_QUESTIONS.md</code> — <em>Spinner signing —
  keys, custody, rotation, recognition revocation</em>). Until then, the only Spinners visible are
  the ones already inside this Cell's <code>~/warp/spinners/</code> directory; see the Installed
  view.
</p>

<style>
  h1 {
    margin: 0 0 0.4rem;
    font-size: 1.6rem;
    font-weight: 600;
    color: var(--gold);
    letter-spacing: 0.02em;
    font-family: var(--font-prose);
  }

  .placeholder {
    color: var(--text-secondary);
    max-width: 60ch;
    line-height: 1.65;
    font-size: 0.95rem;
    margin: 0.85rem 0;
    font-family: var(--font-prose);
    font-style: italic;
  }

  code {
    background: #1a1a1a;
    border: 1px solid #2a2a2a;
    border-radius: 3px;
    padding: 1px 5px;
    font-size: 0.85em;
    color: var(--text-secondary);
    font-style: normal;
    font-family: var(--font-mono);
  }
</style>
